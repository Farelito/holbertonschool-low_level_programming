#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
int dgt;
for(dgt = 0; dgt < 10; dgt++)
printf("%d\n", dgt);
return (0);
}
